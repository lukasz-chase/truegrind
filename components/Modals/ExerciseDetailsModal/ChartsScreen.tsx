import { Exercise } from "@/types/exercises";
import { StyleSheet, Text, View } from "react-native";

const ChartsScreen = ({ exercise }: { exercise: Exercise }) => {
  return (
    <View>
      <Text>Charts</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ChartsScreen;
