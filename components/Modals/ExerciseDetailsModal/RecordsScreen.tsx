import { Exercise } from "@/types/exercises";
import { StyleSheet, Text, View } from "react-native";

const RecordsScreen = ({ exercise }: { exercise: Exercise }) => {
  return (
    <View>
      <Text>Records</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default RecordsScreen;
