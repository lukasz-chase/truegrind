import { Exercise } from "@/types/exercises";
import { StyleSheet, Text, View } from "react-native";

const HistoryScreen = ({ exercise }: { exercise: Exercise }) => {
  return (
    <View>
      <Text>History</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default HistoryScreen;
